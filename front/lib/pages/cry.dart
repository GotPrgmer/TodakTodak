import 'package:flutter/material.dart';

class Cry extends StatelessWidget {
  const Cry({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('음성분석 페이지');
  }
}
